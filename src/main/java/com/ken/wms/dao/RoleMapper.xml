<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ken.wms.dao.RoleMapper">
	
	<resultMap type="com.ken.wms.domain.Role" id="role">
		<id property="id" column="ROLE_ID"/>
		<result property="roleName" column="ROLE_NAME"/>
		<result property="roleDesc" column="ROLE_DESC"/>
		<result property="rolePrefix" column="ROLE_URL_PREFIX"/>
	</resultMap>
	
	<select id="selectById" resultMap="role">
		select
			ROLE_ID,
			ROLE_NAME,
			ROLE_DESC,
			ROLE_URL_PREFIX
		from
			wms_roles
		where
			ROLE_ID = #{id}
	</select>
	
	<select id="selectByUserId" resultMap="role">
		select
			r.ROLE_ID,
			r.ROLE_NAME,
			r.ROLE_DESC,
			r.ROLE_URL_PREFIX
		from 
			wms_roles r,wms_user_role ur
		where
			r.ROLE_ID = ur.ROLE_ID and
			ur.USER_ID = #{userId}
	</select>
	
	<select id="selectAll" resultMap="role">
		select
			ROLE_ID,
			ROLE_NAME,
			ROLE_DESC,
			ROLE_URL_PREFIX
		from
			wms_roles
	</select>
	
	<insert id="insert">
		insert into wms_roles(ROLE_NAME,ROLE_DESC,ROLE_URL_PREFIX)
		values(#{roleName},#{roleDesc},#{rolePrefix})
	</insert>
	
	<update id="update">
		update wms_roles
		set
			ROLE_NAME = #{roleName},
			ROLE_DESC = #{roleDesc},
			ROLE_URL_PREFIX = #{rolePrefix}
		where
			ROLE_ID = #{id}
	</update>
	
	<delete id="delete">
		delete from wms_roles
		where ROLE_ID = #{id}
	</delete>
	
</mapper>